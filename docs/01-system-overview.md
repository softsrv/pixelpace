# System Overview

## Purpose

A production-ready web application built for longevity and maintainability. The stack is chosen to minimize operational complexity: one compiled binary, one database, no runtime dependencies beyond the OS. All dynamics are handled server-side with HTMX partial swaps rather than a heavy frontend framework.

## Technology Stack

| Layer | Choice | Rationale |
|---|---|---|
| Language | Go (stdlib-first) | Single compiled binary, excellent concurrency, strong stdlib |
| Frontend dynamics | HTMX 4.0 | Server-rendered partials; no build step, no JS framework |
| CSS | TailwindCSS + DaisyUI | Utility-first; DaisyUI provides themed components without custom CSS |
| Templating | `html/template` (stdlib) | Auto-escaping by default; prevents XSS at the framework level |
| Database | PostgreSQL | Reliable, well-supported; accessed via `pgxpool` |
| DB access | sqlc + pgx/v5 | Type-safe, compiled queries; no ORM magic |
| Migrations | golang-migrate | SQL-first, version-controlled schema changes |
| Auth tokens | JWT (HS256) + opaque refresh tokens | Short-lived access + long-lived non-rotating refresh |
| UUID generation | google/uuid (UUIDv7) | Time-ordered, collision-resistant IDs |
| Password hashing | bcrypt (cost 12) | Industry standard; cost factor tuned for security/performance |
| Logging | `log/slog` (stdlib) | Structured; JSON in production, text in development |
| Deployment | Docker (multi-stage) | Minimal runtime image; secrets via env vars only |

## Module Boundaries

The codebase is organized around a strict layering rule:

```
HTTP Handlers  →  Service Layer (internal/app/)  →  Repository (internal/db/)  →  PostgreSQL
```

- **Handlers** are thin: parse input, call a service method, render output. No business logic.
- **Services** (`internal/app/`) orchestrate: validate business rules, call one or more repository functions, return domain types.
- **Repositories** (`internal/db/`) are generated by sqlc: pure data access, no logic.
- **Domain types** flow upward; database types stay in `internal/db/`.

Cross-cutting concerns (auth, logging, security headers, body-size limit, rate limiting) live in middleware and are composed on the router. There are no CSRF tokens; CSRF is mitigated by `SameSite=Lax` cookies plus same-origin form posts.

## Directory Structure

```
.
├── cmd/
│   └── app/
│       └── main.go              # Bootstrap only: config → DB pool → wire deps → start server
│
├── internal/
│   ├── app/                     # Service layer — business logic and orchestration
│   │   ├── auth_service.go      # Login, logout, token issue/refresh, password reset
│   │   └── user_service.go      # User registration, profile, verification
│   │
│   ├── auth/                    # Auth primitives — JWT, token hashing
│   │   ├── jwt.go               # Issue and validate access tokens
│   │   └── tokens.go            # Refresh/reset/verification token generation, SHA-256 hashing
│   │
│   ├── db/                      # sqlc-generated code (do not edit by hand)
│   │   ├── db.go                # Querier interface and DBTX abstraction
│   │   ├── models.go            # Generated struct types for each table
│   │   └── queries.sql.go       # Generated typed query functions
│   │
│   ├── users/                   # User domain helpers (not the service layer)
│   │   └── validate.go          # Email normalization, password validation
│   │
│   ├── email/                   # Email sending abstraction
│   │   ├── mailer.go            # Interface + SMTP implementation
│   │   └── templates.go         # Email body rendering (text/html)
│   │
│   └── http/
│       ├── middleware/
│       │   ├── auth.go            # JWT validation, user-to-context injection
│       │   ├── bodylimit.go       # Request body-size cap (http.MaxBytesReader)
│       │   ├── ip.go              # Shared ClientIP helper (X-Forwarded-For aware)
│       │   ├── ratelimit.go       # In-memory rate limiter (sync.Map + TTL)
│       │   ├── securityheaders.go # Security headers / CSP
│       │   ├── requestid.go       # Attach unique request ID to context + logs
│       │   └── logging.go         # Structured request/response logging
│       │
│       ├── handlers/
│       │   ├── auth.go          # Login, register, logout, refresh, reset handlers
│       │   ├── sessions.go      # Session list and revocation handlers
│       │   └── health.go        # /health (liveness), /ready (readiness) endpoints
│       │
│       └── router.go            # Route registration, middleware composition
│
├── web/
│   ├── templates/               # html/template files
│   │   ├── base.html            # Root layout (nav, theme, JS includes)
│   │   ├── auth/
│   │   │   ├── login.html
│   │   │   ├── register.html
│   │   │   ├── forgot-password.html
│   │   │   └── reset-password.html
│   │   └── partials/            # HTMX swap targets (form errors, flash messages)
│   │       ├── error.html
│   │       └── flash.html
│   │
│   └── static/
│       ├── css/
│       │   └── app.css          # Tailwind input file (source); output goes to dist/
│       └── js/
│           └── app.js           # Minimal JS: HTMX config, token-refresh listener
│
├── db/
│   ├── migrations/              # golang-migrate numbered SQL files
│   │   ├── 000001_create_users.up.sql
│   │   ├── 000001_create_users.down.sql
│   │   └── ...
│   ├── queries/                 # SQL source files consumed by sqlc
│   │   ├── users.sql
│   │   ├── refresh_tokens.sql
│   │   ├── password_reset_tokens.sql
│   │   └── email_verification_codes.sql
│   └── sqlc.yaml                # sqlc configuration
│
├── Dockerfile
├── .dockerignore
├── Makefile
├── .air.toml                    # Air live-reload config for development
├── .env.example                 # Placeholder env var reference
└── README.md
```

## main.go Responsibilities

`cmd/app/main.go` performs only bootstrap and wiring — no business logic ever touches it:

1. Parse and validate all environment variables into a typed `Config` struct
2. Initialize `pgxpool.Pool` with configured limits
3. Construct the sqlc `Queries` instance
4. Construct service instances, injecting the DB pool
5. Construct the HTTP router, injecting services and middleware
6. Start `http.Server` with the configured timeouts
7. Block on OS signal (`SIGTERM` / `SIGINT`) via `signal.NotifyContext`
8. On signal: log shutdown start, call `server.Shutdown(ctx)` with 30s deadline, stop background workers, close DB pool, log completion

## HTTP Server Timeouts

| Setting | Value |
|---|---|
| ReadTimeout | 10 seconds |
| WriteTimeout | 30 seconds |
| IdleTimeout | 120 seconds |
| ReadHeaderTimeout | 5 seconds |

These values guard against slow-client attacks without penalizing legitimate users on slow connections.
